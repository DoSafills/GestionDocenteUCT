# App environment example
# PostgreSQL connection URL (production/dev)
DATABASE_URL=postgresql+psycopg://usuario:password@localhost:5432/gduct

# Echo SQL statements (0 or 1)
ECHO_SQL=0

# Test database URL (pytest). Uses in-memory SQLite by default.
DATABASE_URL_TEST=sqlite+pysqlite:///:memory:

# --- Optional: Supabase example (uncomment and adjust) ---
# DATABASE_URL=postgresql+psycopg://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres?sslmode=require
